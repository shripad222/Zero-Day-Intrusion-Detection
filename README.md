# Zero-Day Intrusion Detection using Autoencoder

This project implements an unsupervised anomaly detection system to detect zero-day cyber attacks in network traffic.

The model is trained only on benign traffic and identifies anomalies using reconstruction error from an Autoencoder neural network.

Dataset: CIC-IDS2017
Tools: Python, TensorFlow, Scikit-learn, Pandas, Matplotlib
